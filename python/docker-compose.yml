services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: rentagentgh
      POSTGRES_USER: rentagentgh
      POSTGRES_PASSWORD: rentagentgh
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rentagentgh -d rentagentgh"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
